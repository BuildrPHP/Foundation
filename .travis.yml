language: php

git:
  submodules: true

cache:
  directories:
    - $HOME/.composer/cache

php:
  - 5.6
  - 7.0
  - hhvm

before_install:
  - sudo apt-get update -qq
  - composer self-update
  - composer install

script:
 - ./vendor/bin/phing -f build.xml travis
